/**
 * Bài tập 4: Enum trong TypeScript
 */

// Tạo một enum có tên Color với các giá trị Red, Green, Blue.
enum Color {
    "RED",
    "Green",
    "Blue"
}

// Tạo một biến có kiểu Color và gán cho nó giá trị Color.Green.
let color = Color.Green
console.log(color); //result color = 1
